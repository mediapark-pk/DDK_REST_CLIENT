import { ModelType } from './../modelType';
export declare class LocalSchema {
    private key;
    private val;
    constructor(key: ModelType, val: ModelType);
}
export declare class MapObject extends ModelType {
    private localSchema;
    constructor(key: ModelType, value: ModelType);
    getLength(object: any): number;
    read(buffer: any, offset: any): {
        value: {};
        offset: any;
    };
    write(buffer: any, object: any, offset: any): any;
}
