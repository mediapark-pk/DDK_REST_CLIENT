import { ModelType } from './../modelType';
export declare class BufferMap extends ModelType {
    private typeElement;
    private key;
    private val;
    constructor(key?: ModelType, val?: ModelType);
    getLength(data: Map<any, any>): any;
    read(buffer: any, offset: any): {
        value: Map<unknown, unknown>;
        offset: any;
    };
    private writeTypes;
    write(buffer: any, data: Map<any, any>, offset: any): any;
}
