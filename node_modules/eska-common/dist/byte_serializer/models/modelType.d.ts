/// <reference types="node" />
declare class MetaLength {
    readonly type: number;
    head: number;
    body: number;
    constructor();
}
export declare abstract class ModelType {
    length: MetaLength;
    constructor();
    typeId: number;
    protected abstract read(buffer: Buffer, offset: number): {
        offset: number;
        value: any;
    };
    protected abstract write(buffer: Buffer, value: any, offset: number): number;
    create(data: any): Buffer;
    protected writeTypeId(buffer: Buffer, offset: number): number;
    abstract getLength(value?: any): number;
}
export {};
