import { SocketType } from '../server/types';
export declare const validate: (type: SocketType) => (_target: any, _propertyName: string, descriptor: PropertyDescriptor) => {
    value: (message: any, socket: any) => any;
};
