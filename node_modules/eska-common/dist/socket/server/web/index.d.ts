import { SocketRunnable, SocketServer } from '../../server/types';
import { Server, ServerOptions } from 'ws';
import { ISocketMiddleware } from '../../middleware/middleware';
export declare class WebSocketServer extends SocketServer<Server, ServerOptions> implements SocketRunnable {
    constructor(port: number, config?: ServerOptions, heartbeatRate?: number, middleware?: ISocketMiddleware<Server>);
    protected enableHeartbeat(heartbeatRate: number): void;
    run(): void;
    shutdown(): void;
}
