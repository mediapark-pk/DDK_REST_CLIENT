import { Server, ServerOptions } from 'socket.io';
import { ISocketMiddleware } from '../../middleware/middleware';
import { SocketRunnable, SocketServer } from '../types';
export declare class IOSocketServer extends SocketServer<Server, ServerOptions> implements SocketRunnable {
    constructor(port: number, config: ServerOptions, heartbeatRate?: number, middleware?: ISocketMiddleware<Server>);
    run(): void;
    shutdown(): void;
}
