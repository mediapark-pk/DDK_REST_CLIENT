export declare enum MessageType {
    REQUEST = 1,
    RESPONSE = 2,
    EVENT = 3
}
export declare class Message<MessageBody, Code> {
    code: Code;
    headers: {
        id: string;
        type: MessageType;
    };
    body: MessageBody;
    constructor(type: MessageType, code: Code, body: any, id?: string);
}
