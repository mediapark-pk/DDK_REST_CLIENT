import { ApiContainer } from '../server/types';
export declare const API_CONTAINER: ApiContainer;
export declare const API: (apiName: string) => (target: Object, propertyKey: string, descriptor: PropertyDescriptor) => {
    configurable: boolean;
    get(): () => any;
};
