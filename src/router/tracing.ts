import express from 'express';
// import { tracingController} from 'src/controller/tracing';

export const tracingRouter = express.Router();

// tracingRouter.get('/',tracingController.GetTracing);


